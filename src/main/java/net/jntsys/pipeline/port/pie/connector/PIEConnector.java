package net.jntsys.pipeline.port.pie.connector;

/**
 * Интерфейс описывающий структуру, необходимую для соединения с портами импорта/экспорта конвейера.
 * На основе данного интерфейса должны строиться "соединитель" позволящий внешнему (по отношению к конвейеру) компоненту
 * установить соединение с портом конвейера.
 */
public interface PIEConnector {
}
